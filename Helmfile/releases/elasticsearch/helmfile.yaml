---
bases:
  - ../../commons/environments.yaml
---
releases:
  - name: "elasticsearch"
    namespace: {{ .Values.Namespace }}
    labels:
      chart: "elasticsearch"
      repo: "elastic"
    chart: "elastic/elasticsearch"
    version: "7.11.2"
    wait: true
    values:
      - values.yaml
