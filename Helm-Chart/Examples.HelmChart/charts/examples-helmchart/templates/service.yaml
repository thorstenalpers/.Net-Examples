apiVersion: v1
kind: Service
metadata:
  name: {{ include "examples-helmchart.fullname" . }}
  labels:
    {{- include "examples-helmchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "examples-helmchart.selectorLabels" . | nindent 4 }}
